with open ("task2.txt", "r") as file:
 result1 = len(file.readlines())
 print('Количество строк:', result1)

 with open("task2.txt", "r") as file:
   for line in file:
    p = (len(line.split(' '))
      print( p)

 # print('Количество слов в строке:',p)
#   result2 = len(file.readline())
# print(result2)
